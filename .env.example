NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=
GROQ_API_KEY=
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Public support, privacy, and responsible-disclosure contact.
SUPPORT_EMAIL=contact@crazy-loops.com
# Canonical padded standard Base64 for exactly 32 random bytes (44 characters).
FLOWMIND_CREDENTIAL_MASTER_KEY=
FLOWMIND_RATE_LIMIT_SECRET=
# At least 32 random characters. Signs generic webhook endpoints server-side.
FLOWMIND_CONNECTOR_ENDPOINT_SECRET=
# Google connector server configuration. Client secrets and service-account
# credentials must never use a NEXT_PUBLIC_ prefix.
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
# Browser-used Google Picker configuration, returned only to authenticated
# users. Restrict the API key to the production web origin and Picker API.
GOOGLE_PICKER_API_KEY=
# Numeric Google Cloud project number used as the Picker App ID.
GOOGLE_PICKER_APP_ID=
# Exact OIDC audience and service-account email configured on the authenticated
# Pub/Sub push subscription.
GOOGLE_PUBSUB_AUDIENCE=
GOOGLE_PUBSUB_SERVICE_ACCOUNT=
# Full topic resource name: projects/<project-id>/topics/<topic-name>
GOOGLE_GMAIL_PUBSUB_TOPIC=
# Slack public app (server-only). Enable PKCE, and configure the OAuth redirect
# plus signed Events API endpoint documented in docs/CONNECTORS_SLACK_NOTION.md.
FLOWMIND_CONNECTOR_SLACK_CLIENT_ID=
FLOWMIND_CONNECTOR_SLACK_CLIENT_SECRET=
FLOWMIND_CONNECTOR_SLACK_SIGNING_SECRET=
# Notion public connection (server-only). The webhook verification token is the
# one-time token delivered by Notion while registering the production endpoint.
FLOWMIND_CONNECTOR_NOTION_CLIENT_ID=
FLOWMIND_CONNECTOR_NOTION_CLIENT_SECRET=
FLOWMIND_CONNECTOR_NOTION_WEBHOOK_VERIFICATION_TOKEN=
# Temporary, server-only, at least 32 random characters. Add only while
# capturing the initial Notion webhook token, then remove and redeploy.
FLOWMIND_CONNECTOR_NOTION_SETUP_SECRET=
FLOWMIND_AI_EXECUTION_MODEL=openai/gpt-oss-20b
FLOWMIND_AI_EXECUTION_TIMEOUT_MS=20000
FLOWMIND_AI_MAX_INPUT_CHARS=20000
FLOWMIND_AI_MAX_OUTPUT_TOKENS=1000
# Required in the browser for Supabase Auth CAPTCHA-protected signup, sign-in,
# and recovery. Store the matching secret in Supabase Auth Bot and Abuse
# Protection; never expose that secret as NEXT_PUBLIC_*.
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
# Used only when a hosted public form is explicitly set to Turnstile mode.
TURNSTILE_SECRET_KEY=
# Required by Vercel Cron to authenticate the internal maintenance route.
CRON_SECRET=
# Dedicated bearer secret used only by Supabase Cron to dispatch due schedules.
SCHEDULE_DISPATCH_SECRET=
# Optional release label outside Vercel; Vercel deployments use the Git SHA.
FLOWMIND_RELEASE=

# Delegated execution foundation (server-only; disabled unless both flags are exactly true)
DELEGATED_EXECUTION_ENABLED=false
ACTIVEPIECES_EXECUTOR_ENABLED=false
ACTIVEPIECES_BRIDGE_URL=
ACTIVEPIECES_BRIDGE_SECRET=
ACTIVEPIECES_BRIDGE_TIMEOUT_MS=

# D1.6 Connector Runner. All values are server-only. Both execution flags must
# remain false until the isolated runner and real persistence canary are accepted.
CONNECTOR_RUNNER_EXECUTION_ENABLED=false
CONNECTOR_RUNNER_URL=
# Dedicated transport HMAC secret; never reuse the Activepieces bridge secret.
CONNECTOR_RUNNER_SECRET=
CONNECTOR_RUNNER_TIMEOUT_MS=10000
# Dedicated AES-256-GCM wrapping keys. Configure the active version on both
# CrazyLoops and the runner; retain prior versions only during bounded rotation.
CONNECTOR_RUNNER_WRAP_KEY_ACTIVE_VERSION=
CONNECTOR_RUNNER_WRAP_KEY_V1=
# Runner-host-only settings. Redis must not be internet-accessible.
CONNECTOR_RUNNER_REDIS_URL=
CONNECTOR_RUNNER_REDIS_TIMEOUT_MS=1000
CONNECTOR_RUNNER_HOST=127.0.0.1
CONNECTOR_RUNNER_PORT=8788
CONNECTOR_RUNNER_ADAPTER_TIMEOUT_MS=10000
